import React from "react";

// TODO Change: <Preload runners={[runner1, runner2]} />
const Utils: React.FC = (props) => {
  return <script src="https://apis.google.com/js/platform.js" defer />;
};

export default Utils;
