const size = {
  mobile: "320px",
  desktop: "600px",
};

const theme = {
  mainColor: "#0a4297",
  mobile: `(max-width: ${size.mobile})`,
  desktop: `(min-width: ${size.desktop})`,
};

export default theme;
